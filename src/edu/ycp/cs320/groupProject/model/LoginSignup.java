package edu.ycp.cs320.groupProject.model;

public class LoginSignup {
	//hey
	// Do not need a model for loginSignUp
	// It is not an object
	
	/*
	public boolean checkNameLength(User u)
	{
		if (u.getName().length() > 32 )
		{
			//make this an exception
			System.out.print("Username must be 32 characters or less");
			return false;
		}
		else return true;
		
	}
	
	public boolean checkPasswordLength(User u)
	{
		if (u.getPassword().length() > 32 )
		{
			//make this an exception
			System.out.print("Password must be 32 characters or less");
			return false;
		}
		
		else{
			return true;
		}
		
	}
	
	public boolean checkIfNameTaken(User u)
	{
		if ( u.CreateAccount().equals( u.getName()) )
				{
			return true;
				}
		
		else return false;
	}
	
	*/
}
